//Use filter to create an array of only the positive numbers from a mixed array.

var arr = [-1, 2, 1, 4, -9, -2, 4, 6, -7, 8];

var positives = arr.filter((num) => num > 0);

console.log(positives);
